metadata_path: data/metadata_full.csv
data_root: .
data_dir: all_selection
repo_root: .

train_split: train
val_split: val
batch_size: 1
num_workers: 0
normalize: true
pin_memory: true
input_shape: [2, 1723, 501]

ensure_data: false
use_dvc: false
dvc_remote: data_storage
huggingface_repo_id: FAKIrik/Seismo_datasets
allow_huggingface_fallback: true

class_id_to_crack_count:
  0: 3
  1: 4
  2: 5
